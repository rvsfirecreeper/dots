#!/bin/bash

if [ "$1" == "-A" ]; then
	paru --mode aur -S "$2"
elif [ "$1" == "-Ayu" ]; then
	paru --mode aur -Syu
elif [ "$1" == "-As" ]; then
	paru --mode aur -Ss "$2"
elif [ "$1" == "-Ay" ]; then
	paru --mode aur -Ay "$2"
else
	sudo pacman "$@" 
fi

